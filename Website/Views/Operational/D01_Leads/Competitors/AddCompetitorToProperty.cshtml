@model MyVoltage.Models.OperationalModels.D01_Leads.D01_LeadsModels.Competitors_AddToPropertyModel
@using MyVoltage.Extensions;
@{
    ViewData["Title"] = $"{MyVoltage.Data.SecureAreaEnum.D01_Leads_Competitors.GetDescription()} - Add to property";
}
<div id="loaderContent" class="loader-overlay-content">
    <div class="loader">
        <div class="text-orange text-center">
            <i class="fas fa-spinner fa-spin fa-2x"></i>&nbsp;Loading...
        </div>
    </div>
</div>
<div class="row mb-4">
    <div class="col">

        <div class="card p-md-1 mv-gradient">
            @if (Model != null && Model.IsSuccess)
            {
                <div class="card-header">
                    Success!
                </div>
                <div class="card-body">
                    Competitor successfully added. Click Ok to update Competitor Details
                </div>
                <div class="card-footer">
                    <a href="/operational/D01_Leads/D01_Leads_Competitors_Edit/@Model.ResultCompetitorID" class="btn btn-outline-primary-sm">Back to Competitor</a>
                    <a href="/operational/D01_Leads/D01_Leads_Properties_Edit/@Model.ResultPropertyID" class="btn btn-outline-primary-sm">Back to Property</a>
                </div>
            }
            else
            {
                <div class="card-header">
                </div>
                <form asp-action="D01_Leads_Competitors_AddToProperty" enctype="multipart/form-data">
                    <div class="card-body">
                        <input type="hidden" id="hidden-CompetitorID" name="hidden-CompetitorID" value="@(Model.ResultCompetitorID)">
                        <input type="hidden" id="hidden-PropertyID" name="hidden-PropertyID" value="@(Model.ResultPropertyID)">

                        <div class="row mb-md-2">
                            <div class="col-6">
                                <label asp-for="CompetitorID" class="control-label"></label>
                            </div>
                            <div class="col-6">
                                @if (string.IsNullOrEmpty(Model.CompetitorID))
                                {
                                    <input type="text" asp-for="CompetitorID" class="form-control" />
                                }
                                else
                                {
                                    <input type="text" asp-for="CompetitorID" class="form-control" disabled="disabled" />
                                }
                                <span asp-validation-for="CompetitorID" class="text-red"></span>
                            </div>
                        </div>

                        <div class="row mb-md-2">
                            <div class="col-6">
                                <label asp-for="PropertyID" class="control-label"></label>
                            </div>
                            <div class="col-6">
                                @if (string.IsNullOrEmpty(Model.PropertyID))
                                {
                                    <input type="text" asp-for="PropertyID" class="form-control" />
                                }
                                else
                                {
                                    <input type="text" asp-for="PropertyID" class="form-control" disabled="disabled" />
                                }
                                <span asp-validation-for="PropertyID" class="text-red"></span>
                            </div>
                        </div>

                    </div>
                    <div class="card-body">
                        <div class="row mb-md-2">
                            <div class="col-6">
                                <a href="/operational/D01_Leads/D01_Leads_Competitors" class="btn btn-outline-primary-sm"><i class="fas fa-chevron-left"></i>&nbsp;Back to Competitors</a>
                                <a href="/operational/D01_Leads/D01_Leads_Properties" class="btn btn-outline-primary-sm"><i class="fas fa-chevron-left"></i>&nbsp;Back to Properties</a>
                            </div>
                            <div class="col-6 text-right">
                                <a href="#" class="btn btn-outline-primary-sm" onclick="DoPost();"><i class="fas fa-save"></i>&nbsp;Add Link</a>
                            </div>
                        </div>
                    </div>

                    <div class="card-footer">
                    </div>
                </form>

            }
        </div>

    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function () {
            // Hidden field to store the selected value
            var hiddenFieldC = $("#hidden-CompetitorID");

            $("#CompetitorID").autocomplete({
                source: function (request, response) {
                    $.ajax({
                        url: '/operational/D01_Leads/D01_Leads_Competitors_AddToProperty_SearchCompetitors',
                        type: 'POST',
                        data: {
                            Prefix: request.term
                        },
                        success: function (data) {
                            response(data);
                        },
                        error: function (xhr, status, error) {
                            console.error(error);
                        }
                    });
                },
                select: function (event, ui) {
                    // Set the selected value in the hidden field
                    hiddenFieldC.val(ui.item.value);
                    // Display the selected text in the text box
                    $(this).val(ui.item.label);
                    return false;
                }
            });
            // Hidden field to store the selected value
            var hiddenFieldP = $("#hidden-PropertyID");

            $("#PropertyID").autocomplete({
                source: function (request, response) {
                    $.ajax({
                        url: '/operational/D01_Leads/D01_Leads_Competitors_AddToProperty_SearchPropertys',
                        type: 'POST',
                        data: {
                            Prefix: request.term
                        },
                        success: function (data) {
                            response(data);
                        },
                        error: function (xhr, status, error) {
                            console.error(error);
                        }
                    });
                },
                select: function (event, ui) {
                    // Set the selected value in the hidden field
                    hiddenFieldP.val(ui.item.value);
                    // Display the selected text in the text box
                    $(this).val(ui.item.label);
                    return false;
                }
            });
        });

        function DoPost() {
            $("#loaderContent").css("display", "block");

            $.post("/operational/D01_Leads/D01_Leads_Competitors_AddToProperty",
                {
                    CompetitorID: $("#hidden-CompetitorID").val(),
                    PropertyID: $("#hidden-PropertyID").val()
                })
                .done(function (data) {
                    $("#loaderContent").css("display", "none");
                    if (data == "true") {
                        alert("Success");
                        $("#loaderContent").css("display", "block");
                        if ('@Model.PropertyID' != '') {
                            document.location.href = "/operational/D01_Leads/D01_Leads_Properties_Edit/@Model.ResultPropertyID";
                        }
                        else if ('@Model.CompetitorID' != '') {
                            document.location.href = "/operational/D01_Leads/D01_Leads_Competitors_Edit/@Model.ResultCompetitorID";
                        }
                        //window.location.reload();
                    }
                    else {
                        alert("There was a problem completing requested action.");
                    }
                });
        }

    </script>
}
