@model MyVoltage.Models.OperationalModels.M_TechnicianDispatch.M_TechnicianDispatchModels.M_TechnicianDispatch_VehicleOverviewModel
@{
    ViewData["Title"] = "Technician Dispatch - Vehicle Overview";
}
@inject MyVoltage.Services.OperationalProvider operationalProvider;
@using MyVoltage.Extensions;

<style>
    /* Always set the map height explicitly to define the size of the div
        * element that contains the map. */
    #map {
        height: 100%;
        background-color: lightblue;
    }
    /* Optional: Makes the sample page fill the window. */
    html, body {
        height: 100%;
        margin: 0;
        padding: 0;
    }
</style>
<div id="map" class="mt-4" style="height:600px"></div>
<div style="clear:both; height:50px"></div>

@section Scripts {
    <script>
        function initMap() {
            var mapOptions = {
                zoom: 12,
                center: new google.maps.LatLng(-25.8092082, 28.2957713),
                mapTypeId: 'roadmap'
            };
            var map = new google.maps.Map(document.getElementById('map'), mapOptions);

            $.ajax({
                type: "GET",
                async: true,
                url: "/operational/M_TechnicianDispatch/M_TechnicianDispatch_VehicleOverview/getmapmarkers",
                dataType: "xml",
                success:
                    function (xml) {
                        var markers = xml.documentElement.getElementsByTagName("Marker");

                        for (var i = 0; i < markers.length; i++) {
                            var lat = markers[i].getElementsByTagName('Lat')[0].innerHTML;
                            var long = markers[i].getElementsByTagName('Long')[0].innerHTML
                            var name = markers[i].getElementsByTagName('Name')[0].innerHTML;
                            var latLng = new google.maps.LatLng(lat, long);
                            var marker = new google.maps.Marker({
                                position: latLng,
                                map: map,
                                label: name
                            });
                            //var infowindow = new google.maps.InfoWindow({
                            //    content: name
                            //});

                            //marker.addListener("click", function () {
                            //    infowindow.open(map, marker);
                            //});

                        }
                    }
            });

        }
    </script>
    <script async defer
            src="https://maps.googleapis.com/maps/api/js?key=AIzaSyCZrm-r6PKi_zpTM2-iT-K4yFG-7y_cEOk&callback=initMap">
    </script>
}