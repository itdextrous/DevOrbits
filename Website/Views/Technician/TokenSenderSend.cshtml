@model MyVoltage.Models.TechnicianViewModels.TokenSenderSendViewModel
@{
    ViewData["Title"] = "Clear Tamper";
}

<div class="row mt-0 mt-md-5">
    <div class="col-12 col-md-6">
        <h3>@ViewData["Title"]</h3>
    </div>
</div>
<form asp-action="TokenSender" enctype="multipart/form-data">
    <div class="row mb-4">
        <div class="col">

            <div class="card p-md-4">
                <div class="card-content">
                    @*@if (!Model.IsSuccess.HasValue)
                        {
                            <div class="row">
                                Please enter Clear Tamper OTP sent to @Model.PhoneNumber
                            </div>

                            <div class="row">
                                <div class="col"><label asp-for="OTP" class="control-label"></label>:</div>
                                <div class="col">
                                    <input class="form-group" asp-for="OTP" />
                                    <span asp-validation-for="OTP" class="text-red"></span>
                                </div>
                            </div>
                            <div class="row">
                                <input type="submit" value="Send Me Clear Tamper" class="btn btn-default" />
                            </div>
                        }
                        else
                        {*@
                    @if (Model.IsSuccess)
                    {
                        <div class="row">
                            @Model.Token - @Model.TokenType has been sent to @Model.Serial
                        </div>
                    }
                    else
                    {
                        <div class="row">
                            Already redemmed or unavailable.
                        </div>
                    }
                    <div class="row">
                        <a href="/technician/TokenSender" class="btn btn-default">Ok</a>
                    </div>
                    @*}*@
                </div>
            </div>

        </div>
    </div>
</form>
