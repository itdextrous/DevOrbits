@model MyVoltage.Models.OperationalModels.J_Finance.J_FinanceModels.J_Finance_CigicellWeeklyDetailsModel
@inject MyVoltage.Services.OperationalProvider operationalProvider;
@{
    ViewData["Title"] = MyVoltage.Data.SecureAreaEnum.J_Finance_CigicellWeeklyDetails.GetDescription();
    if (operationalProvider.CompanyID > 0)
    {
        ViewData["Title"] = MyVoltage.Data.SecureAreaEnum.J_Finance_CigicellWeeklyDetails.GetDescription() + " - " + operationalProvider.CompanyName;
    }
}
@using MyVoltage.Extensions;
<div id="loaderContent" class="loader-overlay-content">
    <div class="loader">
        <div class="text-orange text-center">
            <i class="fas fa-spinner fa-spin fa-2x"></i>&nbsp;Loading...
        </div>
    </div>
</div>
<div class="row mb-4">
    <div class="col">
        <div class="card p-md-1 mv-gradient">
            <div class="card-header">
            </div>
            <div class="card-body">
                <div class="row mb-md-2">
                    <div class="col-12">
                        From:
                        <input type="date" name="fromDate" id="fromDate" value="@(Model.FromDate.ToDateShort())" readonly="readonly" disabled="disabled" />
                        &nbsp;
                        To:
                        <input type="date" name="toDate" id="toDate" value="@(Model.ToDate.ToDateShort())" readonly="readonly" disabled="disabled" />
                    </div>
                </div>

            </div>

            <div class="card-footer">

                @*                <div class="row mb-md-2">
                    <div class="col-12 text-left">
                    <a href="/operational/J_Finance/J_Finance_CigicellWeeklyDetails" class="btn btn-outline-primary-sm"><i class="fas fa-undo"></i>&nbsp;Clear</a>
                    &nbsp;
                    <a onclick="RedirectToFilter();" href="#" class="btn btn-outline-primary-sm"><i class="fas fa-search"></i>&nbsp;Filter</a>
                    </div>
                    </div>
                *@
            </div>
        </div>
    </div>
</div>

<table class="table table-sm table-striped table-hover table-bordered">
    <thead>
        <tr>
            <th scope="col" class="text-nowrap">Company Name</th>
            <th scope="col" class="text-nowrap text-right">Paid Amount</th>
            <th scope="col" class="text-nowrap text-right">Vending Amount</th>
            <th scope="col" class="text-nowrap text-right">Nett Amount Received</th>
            <th scope="col" class="text-nowrap"></th>
            <th scope="col" class="text-nowrap text-right">Paid %</th>
            <th scope="col" class="text-nowrap text-right">Vending %</th>
            <th scope="col" class="text-nowrap text-right">Nett % Received</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.J_Finance_CigicellWeeklyDetailsProductItems)
        {
            string rowClass = "";
            if (Math.Round(item.VendingPerc, 2) != -4.60m)
            {
                rowClass = "table-danger";
            }
            <tr class="@Html.Raw(rowClass)" style="cursor:pointer;" onclick="RedirectToDetails('@item.CompanyID');">
                <td scope="col" class="text-nowrap">@item.CompanyName</td>
                <td scope="col" class="text-nowrap text-right">@item.PaidAmount.ToMoney()</td>
                <td scope="col" class="text-nowrap text-right">@item.VendingAmount.ToMoney()</td>
                <td scope="col" class="text-nowrap text-right">@((item.VendingAmount + item.PaidAmount).ToMoney())</td>
                <td scope="col" class="text-nowrap text-right"></td>
                <td scope="col" class="text-nowrap text-right">@item.PaidPerc.ToMoney()</td>
                <td scope="col" class="text-nowrap text-right">@item.VendingPerc.ToMoney()</td>
                <td scope="col" class="text-nowrap text-right">@item.NettPerc.ToMoney()</td>
            </tr>
        }
        <tr>
            <td scope="col" class="text-nowrap border-table-total">Total</td>
            <td scope="col" class="text-nowrap border-table-total text-right">@Model.PaidAmount.ToMoney()</td>
            <td scope="col" class="text-nowrap border-table-total text-right">@Model.VendingAmount.ToMoney()</td>
            <td scope="col" class="text-nowrap border-table-total text-right">@(Model.NettAmount.ToMoney())</td>
            <td scope="col" class="text-nowrap border-table-total text-right"></td>
            <td scope="col" class="text-nowrap border-table-total text-right">@Model.PaidPerc.ToMoney()</td>
            <td scope="col" class="text-nowrap border-table-total text-right">@Model.VendingPerc.ToMoney()</td>
            <td scope="col" class="text-nowrap border-table-total text-right">@Model.NettPerc.ToMoney()</td>
        </tr>
    </tbody>
</table>

@section Scripts {
<script>
    function RedirectToFilter() {
        $("#loaderContent").css("display", "block");
        document.location.href = '/operational/J_Finance/J_Finance_CigicellWeeklyDetails?from=' + $("#fromDate").val() + '&to=' + $("#toDate").val();
    }
    function RedirectToDetails(companyID) {
        $("#loaderContent").css("display", "block");
        document.location.href = '/operational/changeActiveCompany/' + companyID + '?R=' + encodeURIComponent('/operational/J_Finance/J_Finance_CigicellTransactionDetails?from=' + $("#fromDate").val() + '&to=' + $("#toDate").val());
    }
</script>
}
